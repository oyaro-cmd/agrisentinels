name: agrisentinel_demo
description: "AgriSentinels mobile app for on-device maize disease and pest early warning."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  # Added dependencies for AI and GPS
  image_picker: ^1.0.4
  tflite_flutter: ^0.11.0
  geolocator: ^10.1.0
  image: ^4.1.7
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # Ensure these assets exist in your folder
  assets:
    - assets/model.tflite
    - assets/labels.txt
